general:
  branches:
    only:
      - master

machine:
  ruby:
    version: 2.1.2

dependencies:
  override:

test:
  override:

deployment:
  master:
    branch: master
    commands:
      - scripts/build-sites.sh